// const supertest = require("supertest")
// const router = require("../routes/routes")
// const app = require("../server")

// describe("Route", () => {
//     it("GET /scissor works", async () => {
//         const response = await supertest(app).get("/scissor")
//         expect(response.status).toBe(200)
//     }) 

//     it("GET /login works", async () => {
//         const response = await supertest(app).get("/login")
//         expect(response.status).toBe(200)
//     })

//     it("GET /register works", async () => {
//         const response = await supertest(app).get("/register")
//         expect(response.status).toBe(200)
//     })

    //works without connectEnsureLogin
    // it("GET /urlpage works", async () => {
    //     const response = await supertest(app).get("/scissor/urlpage")
    //     expect(response.status).toBe(200) 
    // })
    // it("GET /history works", async () => {
    //     const response = await supertest(app).get("/scissor/history")
    //     expect(response.status).toBe(200) 
    // }) 
 
})
